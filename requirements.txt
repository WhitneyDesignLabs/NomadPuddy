# NomadPuddy

## System Dependencies
Before running the scripts, ensure you have the following system dependencies installed:

- ffmpeg

On Ubuntu or Debian-based systems (including Raspberry Pi OS), you can install it with:
